import * as PIXI from 'pixi.js';

export const renderArtBox = (
  app: PIXI.Application,
  resources: any,
  image: string
) => {
  // const box = new PIXI.Graphics();
  // box.beginFill(0xff3300);
  // box.lineStyle(4, 0xfeeb77, 1);
  // box.moveTo(36, 78);
  // box.lineTo(36, 376);
  // box.lineTo(449, 376);
  // box.lineTo(449, 78);
  // box.endFill();
  //
  // app.stage.addChild(box);
  //
  // const artTexture = resources[image].texture;
  // let artSprite = new PIXI.TilingSprite(artTexture);
  // artSprite.x = 36;
  // artSprite.y = 78;
  // artSprite.width = 413;
  // artSprite.height = 298;
  // app.stage.addChild(artSprite);
};
